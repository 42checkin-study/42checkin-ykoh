<ul class='list-group'>

  {{#each clusters}}
    <li
      class='list-group-item d-flex justify-content-between align-items-center'
    >
      {{name}}
      <span class='badge bg-primary rounded-pill'>{{capacity}}/{{max}}</span>
    </li>
  {{/each}}

</ul>